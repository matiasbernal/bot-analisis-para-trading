"""Capa de reglas: condiciones atómicas, composición all/any/not, sizing y salidas.

El paquete no reexporta nada a propósito: ``config.py`` necesita solo la lista de
operadores de ``conditions`` y no tiene por qué arrastrar el motor entero.
Importá el módulo que uses (``from tradingbot.strategy import exits``).
"""
